{
	"name": "JATC",
	"version": "0.0.1",

	"devDependencies": {
		"browserify": "latest",
		"watchify": "latest"
	},

	"scripts": {
		"compile": "browserify src/jatc.js -o jatc.js",
		"watch": "watchify src/jatc.js -o jatc.js -v"
	}
}
